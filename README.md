# Лабораторная работа №20: Работа с DOM и событиями в JavaScript

## Основная информация
- **ФИО:** KuzVah
- **Группа:** ИСП-233
- **Дата:** 12.03.2026

## Описание работы
В ходе данной лабораторной работы были изучены основы взаимодействия JavaScript с DOM (Document Object Model). Мы научились находить элементы на веб-странице, изменять их содержимое и стили, обрабатывать действия пользователя (клики, ввод текста, отправку форм), а также динамически создавать новые элементы. Итогом стало создание интерактивного TODO-листа и закрепление навыков валидации данных.

## Что изучили
- Поиск элементов в DOM (`getElementById`, `querySelector`).
- Изменение текста (`textContent`), HTML (`innerHTML`) и стилей (`style`) элементов.
- Обработка событий (`click`, `submit`, `input`) с помощью `addEventListener`.
- Получение и валидация данных из форм.
- Динамическое создание и удаление элементов (`createElement`, `appendChild`, `remove`).
- Принцип делегирования событий для работы с динамически добавленными элементами.

## Структура проекта
Lab20_DOM_Events_FIO/
│
├── README.md # Описание лабораторной работы
├── index.html # Основной файл для шагов 1-9
├── style.css # Файл со стилями (пустой или с минимальными стилями)
├── main.js # Основной скрипт для index.html
├── dynamicElements.html # Файл для шага 10 (динамическое создание)
├── dynamicElements.js # Скрипт для dynamicElements.html
└── img/ # Папка со скриншотами
├── gitPushLab20_DOM_Events_FIO.png
├── step2_domStructureLab20_FIO.png
├── step3_domSelectionLab20_FIO.png
└── ... (остальные скриншоты по шагам)


## Сравнение с C# (WinForms/WPF)

Концепция работы с элементами интерфейса в JavaScript через DOM очень похожа на работу с контролами в C# WinForms или WPF. Ниже представлены основные параллели.

### Сравнение основных операций

| Концепция | C# (Windows Forms) | JavaScript (DOM) |
| :--- | :--- | :--- |
| **Найти элемент** | `Button myButton = this.Controls["myButton"];` | `const myButton = document.getElementById("myButton");` |
| **Изменить текст** | `label1.Text = "Новый текст";` | `label.textContent = "Новый текст";` |
| **Добавить обработчик** | `button1.Click += new EventHandler(HandleClick);` | `button.addEventListener("click", handleClick);` |
| **Создать элемент** | `Button btn = new Button();` | `const btn = document.createElement("button");` |
| **Добавить на форму/в контейнер** | `panel1.Controls.Add(btn);` | `panel.appendChild(btn);` |
| **Удалить элемент** | `panel1.Controls.Remove(btn);` | `btn.remove();` или `panel.removeChild(btn);` |
| **Скрыть элемент** | `button1.Visible = false;` | `button.style.display = "none";` |

## Сравнение с C# (WinForms/WPF)

### Обработка события клика

**C# (WinForms):**
```csharp
private void Button1_Click(object sender, EventArgs e)
{
    label1.Text = "Нажато!";
}

button.addEventListener("click", (e) => {
    label.textContent = "Нажато!";
});

private void SubmitButton_Click(object sender, EventArgs e)
{
    if (string.IsNullOrEmpty(nameTextBox.Text))
    {
        MessageBox.Show("Введите имя!");
        return;
    }
}

form.addEventListener("submit", (e) => {
    e.preventDefault();
    if (!nameInput.value.trim()) {
        alert("Введите имя!");
        return;
    }
    // ... 
});

## Главные отличия
## Главные отличия

| Аспект | C# (WinForms / WPF) | JavaScript (DOM) |
|--------|---------------------|-------------------|
| Компиляция | Да (IL-код) | Нет (интерпретируется браузером) |
| Типизация | Строгая, статическая | Динамическая, слабая |
| Ошибки | Обнаруживаются на этапе компиляции | Обнаруживаются во время выполнения |
| UI-дизайнер | Есть (визуальный drag & drop) | Нет (только код или визуализация в браузере) |
| Платформа | В основном Windows (под .NET Framework) | Любая с браузером (кроссплатформенность) |

## Преимущества и недостатки подходов

### Преимущества DOM (JavaScript) перед WinForms:

- **Кроссплатформенность:** Приложение работает в любом браузере на любой ОС.
- **Простота развертывания:** Не требует установки, достаточно обновить файлы на сервере.
- **Современный интерфейс:** Возможность создавать сложные и красивые интерфейсы с помощью CSS и анимаций.

### Преимущества WinForms перед DOM:

- **Визуальное проектирование:** Быстрая разработка интерфейса через drag & drop.
- **Раннее обнаружение ошибок:** Ошибки типов и многие другие находятся на этапе компиляции.
- **Прямой доступ к API ОС:** Возможность глубокой интеграции с функциями Windows.

## Вывод

В ходе работы были изучены основные принципы работы с DOM в JavaScript. Несмотря на различия в подходах и средах выполнения, задачи управления элементами интерфейса в JS и C# WinForms решаются схожим образом. DOM предоставляет мощные и гибкие инструменты для создания интерактивных веб-приложений, работающих на любой платформе.